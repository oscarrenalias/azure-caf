name: rag-app
services:
  rag-agent:
    host: azure.ai.agent
    project: agent
    language: python
    kind: hosted
    docker:
      context: ../
      path: ./Dockerfile
      remoteBuild: false
