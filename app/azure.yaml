name: rag-app
services:
  rag-agent:
    name: rag-agent
    host: azure.ai.agent
    project: agent
    language: python
    kind: hosted
    docker:
      context: ../
      path: ./Dockerfile
      remoteBuild: false
    protocols:
      - protocol: responses
        version: 2.0.0
