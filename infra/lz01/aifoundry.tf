# AI Foundry has been moved to infra/lz-platform/ as a platform-managed resource.
# Workload apps in this LZ should call the APIM AI Gateway instead of AI Foundry directly.
# Gateway URL: https://<apim-name>.azure-api.net/openai
# Use the APIM product subscription key as the api-key in SDK calls.
